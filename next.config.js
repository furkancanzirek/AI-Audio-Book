/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: false,
    images: {
        domains: ['avatars.githubusercontent.com',"s1.nyt.com","www.designforwriters.com"],
    },
    
}

module.exports = nextConfig
